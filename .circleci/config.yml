version: 2
jobs:
  build:
    docker:
      - image: circleci/openjdk:8-jdk-browsers
      - image: circleci/postgres:9.6.3-alpine
        enviroment:
          POSTGRES_USER: root
          POSTGRES_: cicle_test
    steps:
      - checkout
      - run: echo "Hello CircleCI!"
      - run:
        name: Waite for DB
        command: dockerrize -wait tcp://127.0.0.1:5432 -timeout 120s
